from enum import Enum, auto


class Aggregator(Enum):
    SUM = auto
    AVERAGE = auto


class Functions(Enum):
    DOT_PRODUCT = auto
    MULTIPLE_DOT_PRODUCT = auto
    SUM = auto
    AVERAGE = auto
